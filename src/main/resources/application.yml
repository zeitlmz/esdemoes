server:
  port: 92

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myschool?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    auto-mapping-behavior: FULL
    map-underscore-to-camel-case: true
    cache-enabled: true
  mapper-locations: classpath:mapper/*.xml
